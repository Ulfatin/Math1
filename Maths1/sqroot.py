# Program to read a number at run time and find its square root and ceiling value using math library functions. 

import math
print("Program to calculate the Square root and absolute Value")
num = eval(input("Enter a number: "))
print(math.sqrt(num))
print(math.ceil(num))
